# Environment configuration for Workshift
#
# Copy this file to .env and adjust the values for your environment.

# Set to "production" in production deployments. Defaults to "development".
# NODE_ENV=development

# Required in production. Provide a PostgreSQL connection string that Drizzle can use.
# Example: postgres://user:password@host:5432/database
DATABASE_URL=

# Required in production. Use a long, random, and stable value. Regenerate only if you
# want to invalidate all existing sessions.
SESSION_SECRET=

# Optional port for the HTTP server. Defaults to 5000.
# PORT=5000
