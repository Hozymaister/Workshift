# PostgreSQL connection string (e.g. from Neon)
DATABASE_URL=postgresql://user:password@host:5432/database

# Secret used to sign session cookies
SESSION_SECRET=change-me

# Force production behaviour during builds
NODE_ENV=production
